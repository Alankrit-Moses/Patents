===== EVALUATOR CALL 1 / EXPERIMENT 1 =====
===== MESSAGE 1: SYSTEM =====
You are an evaluation judge blind to the generation setup. Reason internally, apply the alignment rubric strictly, and output only the requested JSON score.

===== MESSAGE 2: USER =====
Score how well this exact report excerpt instantiates the supplied mathematical pattern definition.

Mathematical pattern definition:
Let E = {e1, e2, ..., en} be a set of comparable entities and let m be an attribute measured over time. Let v(e, m, t) denote the value of entity e for attribute m at time t.

For a shared interval [t_start, t_end], where n = t_end - t_start and v(e, m, t_start) > 0, define the compound annual growth rate of e as:

CAGR(e, m; t_start, t_end) = (v(e, m, t_end) / v(e, m, t_start))^(1 / n) - 1

A comparative growth pattern holds when there exist two entities ei and ej such that:

CAGR(ei, m; t_start, t_end) - CAGR(ej, m; t_start, t_end) > epsilon

where epsilon >= 0 is a threshold for a meaningful growth-rate difference.

An entity ei is a growth leader within E when:

CAGR(ei, m; t_start, t_end) = max over e in E of CAGR(e, m; t_start, t_end).

All compared entities must use the same attribute, aggregation method, start year, and end year. An entity with a zero or missing starting value has an undefined CAGR and is excluded from the comparison.


Candidate report excerpt:
Over the past five years, India has shown a remarkable surge in patenting activity related
to heavy-duty energy infrastructure, rising from 39 publications in 2019 to 449 in 2024. This
corresponds to a compound annual growth rate of more than 60%, making India by far the
fastest-growing innovation location in this field.

Use this alignment scale: 5 = clear and complete instantiation; 4 = valid instantiation with minor ambiguity or omitted context; 3 = partial or uncertain instantiation; 2 = weakly related but missing central conditions; 1 = not an instantiation. Judge semantic pattern alignment, not wording similarity. The saved example collection is non-exhaustive and must not be considered.

Return only a JSON object with exactly one key named "score", whose value is the integer from 1 to 5 that you selected. Do not return a rationale.


===== EVALUATOR CALL 2 / EXPERIMENT 1 =====
===== MESSAGE 1: SYSTEM =====
You are an evaluation judge blind to the generation setup. Reason internally, apply the alignment rubric strictly, and output only the requested JSON score.

===== MESSAGE 2: USER =====
Score how well this exact report excerpt instantiates the supplied mathematical pattern definition.

Mathematical pattern definition:
Let E = {e1, e2, ..., en} be a set of comparable entities and let m be an attribute measured over time. Let v(e, m, t) denote the value of entity e for attribute m at time t.

For a shared interval [t_start, t_end], where n = t_end - t_start and v(e, m, t_start) > 0, define the compound annual growth rate of e as:

CAGR(e, m; t_start, t_end) = (v(e, m, t_end) / v(e, m, t_start))^(1 / n) - 1

A comparative growth pattern holds when there exist two entities ei and ej such that:

CAGR(ei, m; t_start, t_end) - CAGR(ej, m; t_start, t_end) > epsilon

where epsilon >= 0 is a threshold for a meaningful growth-rate difference.

An entity ei is a growth leader within E when:

CAGR(ei, m; t_start, t_end) = max over e in E of CAGR(e, m; t_start, t_end).

All compared entities must use the same attribute, aggregation method, start year, and end year. An entity with a zero or missing starting value has an undefined CAGR and is excluded from the comparison.


Candidate report excerpt:
IRENA (2026). Near-700 GW Surge in 2025 Proves Renewable Energy Resilience

Use this alignment scale: 5 = clear and complete instantiation; 4 = valid instantiation with minor ambiguity or omitted context; 3 = partial or uncertain instantiation; 2 = weakly related but missing central conditions; 1 = not an instantiation. Judge semantic pattern alignment, not wording similarity. The saved example collection is non-exhaustive and must not be considered.

Return only a JSON object with exactly one key named "score", whose value is the integer from 1 to 5 that you selected. Do not return a rationale.


===== EVALUATOR CALL 3 / EXPERIMENT 1 =====
===== MESSAGE 1: SYSTEM =====
You are an evaluation judge blind to the generation setup. Reason internally, apply the alignment rubric strictly, and output only the requested JSON score.

===== MESSAGE 2: USER =====
Score how well this exact report excerpt instantiates the supplied mathematical pattern definition.

Mathematical pattern definition:
Let E = {e1, e2, ..., en} be a set of comparable entities and let m be an attribute measured over time. Let v(e, m, t) denote the value of entity e for attribute m at time t.

For a shared interval [t_start, t_end], where n = t_end - t_start and v(e, m, t_start) > 0, define the compound annual growth rate of e as:

CAGR(e, m; t_start, t_end) = (v(e, m, t_end) / v(e, m, t_start))^(1 / n) - 1

A comparative growth pattern holds when there exist two entities ei and ej such that:

CAGR(ei, m; t_start, t_end) - CAGR(ej, m; t_start, t_end) > epsilon

where epsilon >= 0 is a threshold for a meaningful growth-rate difference.

An entity ei is a growth leader within E when:

CAGR(ei, m; t_start, t_end) = max over e in E of CAGR(e, m; t_start, t_end).

All compared entities must use the same attribute, aggregation method, start year, and end year. An entity with a zero or missing starting value has an undefined CAGR and is excluded from the comparison.


Candidate report excerpt:
IRENA, COP30 and GRA (2025). Delivering on the UAE Consensus: Tracking Progress Toward Tripling Renewable
Energy Capacity and Doubling Energy Efficiency by 2030.

Use this alignment scale: 5 = clear and complete instantiation; 4 = valid instantiation with minor ambiguity or omitted context; 3 = partial or uncertain instantiation; 2 = weakly related but missing central conditions; 1 = not an instantiation. Judge semantic pattern alignment, not wording similarity. The saved example collection is non-exhaustive and must not be considered.

Return only a JSON object with exactly one key named "score", whose value is the integer from 1 to 5 that you selected. Do not return a rationale.

