===== EVALUATOR CALL 1 / EXPERIMENT 3: M =====
===== MESSAGE 1: SYSTEM =====
You are an evaluation judge blind to the generation setup. Reason internally, apply the alignment rubric strictly, and output only the requested JSON score.

===== MESSAGE 2: USER =====
Score the semantic and operational alignment of a generated mathematical pattern definition with the canonical mathematical definition. Equivalent notation and wording are acceptable; a specific example, CSV/table, or non-general rule is not a valid replacement.

Canonical M:
Let E = {e1, e2, ..., en} be a set of comparable entities and let m be an attribute measured over time. Let v(e, m, t) denote the value of entity e for attribute m at time t.

For a shared interval [t_start, t_end], where n = t_end - t_start and v(e, m, t_start) > 0, define the compound annual growth rate of e as:

CAGR(e, m; t_start, t_end) = (v(e, m, t_end) / v(e, m, t_start))^(1 / n) - 1

A comparative growth pattern holds when there exist two entities ei and ej such that:

CAGR(ei, m; t_start, t_end) - CAGR(ej, m; t_start, t_end) > epsilon

where epsilon >= 0 is a threshold for a meaningful growth-rate difference.

An entity ei is a growth leader within E when:

CAGR(ei, m; t_start, t_end) = max over e in E of CAGR(e, m; t_start, t_end).

All compared entities must use the same attribute, aggregation method, start year, and end year. An entity with a zero or missing starting value has an undefined CAGR and is excluded from the comparison.


Generated M:
Given a set of entities E, a start year S, an end year E, and a compound annual growth rate CAGR, the structural behavior can be defined as follows: For each entity e in E, if the CAGR of e from S to E is greater than 0, then e is a growth leader in the period [S, E]. If the CAGR of e from S to E is less than 0, then e is a declining entity in the period [S, E]. If the CAGR of e from S to E is equal to 0, then e is a steady entity in the period [S, E].

Use this alignment scale: 5 = equivalent operational rule; 4 = substantially aligned with a minor omission or ambiguity; 3 = captures part of the rule; 2 = weakly related; 1 = not aligned.

Return only a JSON object with exactly one key named "score", whose value is the integer from 1 to 5 that you selected. Do not return a rationale.


===== EVALUATOR CALL 2 / EXPERIMENT 3: T =====
===== MESSAGE 1: SYSTEM =====
You are an evaluation judge blind to the generation setup. Reason internally, apply the alignment rubric strictly, and output only the requested JSON score.

===== MESSAGE 2: USER =====
Score the semantic alignment of a generated natural-language pattern description with the canonical description. Equivalent wording is acceptable; a specific example or vague topical description is not a valid replacement.

Canonical T:
A comparative growth pattern is a temporal pattern in which comparable entities or categories have meaningfully different compound annual growth rates over the same period. It identifies faster-growing, slower-growing, stagnant, or declining entities independently of their absolute size. The pattern requires a shared measure and time interval for every entity. Unlike a trajectory shift, it does not require a turning point; unlike a top-k pattern, it compares entities by annualized relative growth rather than their final absolute values.


Generated T:
This structural behavior describes the growth, decline, or steadiness of entities over a specific time period, based on their compound annual growth rate. The behavior is comparable across entities within the same time period, and the temporal condition is defined by the start and end years. The aggregation condition is the set of entities, and the boundary condition is the CAGR threshold for growth, decline, or steadiness.

Use this alignment scale: 5 = equivalent clear description; 4 = substantially aligned with a minor omission or ambiguity; 3 = captures part of the pattern; 2 = weakly related; 1 = not aligned.

Return only a JSON object with exactly one key named "score", whose value is the integer from 1 to 5 that you selected. Do not return a rationale.
